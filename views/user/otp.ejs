<!-- --include header-- -->
<%- include("../layout/layout")%>
<%- include("../partials/user-header")%>
<!-- /header -->

<div class="container d-flex justify-content-center align-items-center mt-3 mb-3">
  <div class="container col-lg-4 d-flex flex-column justify-content-center align-items-center rounded-5 p-5 bg-white">
    <h4>Enter OTP Code</h4>
    <form action="/otp" method="post">
      <div id="otp" class="inputs d-flex flex-row justify-content-center mt-2">
        <!-- Your OTP input fields -->
        <!-- Add Bootstrap classes for better spacing -->
        <input class="m-2 text-center form-control rounded" name="first" type="text" id="first" maxlength="1" />
        <input class="m-2 text-center form-control rounded" name="second" type="text" id="second" maxlength="1" />
        <input class="m-2 text-center form-control rounded" name="third" type="text" id="third" maxlength="1" />
        <input class="m-2 text-center form-control rounded" name="fourth" type="text" id="fourth" maxlength="1" />
        
      </div>
      <div class="mt-4 text-center">
        <!-- Align the button to the center -->
        <button type="submit" id="otp-btn" class="btn btn-primary px-4">Verify</button>
      </div>
    </form>
  </div>
</div>


<!-- --include footer-- -->
<%- include("../partials/footer")%>
<!-- /header -->

<style>
  .footer {
    position: fixed;
    text-align: center;
  }
</style>
<script>
    // document.addEventListener("DOMContentLoaded", function() {
    //   const otpInputs = document.querySelectorAll("#otp input");
    //   const otpBtn = document.getElementById("otp-btn");

    //   otpInputs.forEach(function(input, index) {
    //     input.addEventListener("input", function() {
    //       if (index < otpInputs.length - 1 && this.value) {
    //         otpInputs[index + 1].focus();
    //       } else if (index === otpInputs.length - 1 && this.value) {
    //         otpBtn.disabled = false;
    //       }
    //     });

    //     input.addEventListener("keydown", function(event) {
    //       if (event.key === "Backspace" && index > 0 && !this.value) {
    //         otpInputs[index - 1].focus();
    //       }
    //     });
    //   });

    //   otpBtn.addEventListener("click", function(event) {
    //     event.preventDefault();

    //     // Gather the OTP digits and concatenate them
    //     let otpValue = "";
    //     otpInputs.forEach(function(input) {
    //       otpValue += input.value;
    //     });

    //     // Send the OTP value to the server for validation
    //     // Replace 'YOUR_SERVER_ENDPOINT' with your actual server endpoint for OTP validation
    //     fetch("YOUR_SERVER_ENDPOINT", {
    //       method: "POST",
    //       headers: {
    //         "Content-Type": "application/json",
    //       },
    //       body: JSON.stringify({ otp: otpValue }),
    //     })
    //     .then(function(response) {
    //       // Handle the response from the server here (e.g., show success or error messages)
    //       console.log("Response:", response);
    //     })
    //     .catch(function(error) {
    //       console.error("Error:", error);
    //     });
    //   });
    // });
  </script>